
# copy sql script to container 
scp -P 23 script.sql root@localhost:

# ssh into container
ssh -p 23 root@localhost
mysql
\. script.sql
# exit container

# create image from container
docker export container_name > container_db.tar
cat container_db.tar |docker import - mysql/db
