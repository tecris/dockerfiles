
Pre-requisites:
Enable Remote API
 1. Modify /etc/default/docker file to include parameter `-H tcp://0.0.0.0:4243`:
  - `DOCKER_OPTS="-H tcp://0.0.0.0:4243"`

http://localhost:8080/
 .) Manage Jenkins > Configure System 
   - Maven Configuration > Default global settings provider > Global settings file on filesystem
     .) File path: /opt/maven_config/.m2/settings.xml
   - Maven > Maven installations > Add Maven
     .) Deselect 'Install automatically', 
     .) Name: maven3
     .) MAVEN_HOME: /opt/apache-maven-3.3.3
   - Docker Builder 
     .) Docker URL: http://docker.host:4243
     .) Apply > Test connection


===== Continuous Delivery Demo planets =====

-) SCRIPTING

 .) in files directory execute './createJob.sh 192.168.1.69 planets config.xml'

